# This playbook deploys WebCert 

- hosts: webcert-tomcat
  sudo: yes 
  vars:
    # repo - values: nightly, snapshots or releases
    repo: releases
    version: 4.1.0-b31
    config_version: master

  roles:
    - app-resources
    - webcert
