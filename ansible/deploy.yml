# This playbook deploys WebCert 

- hosts: tomcat
  sudo: yes 
  vars:
    # repo - values: snapshot or release
    repo: release
    version: 3.1.0-RC2
    config_version: release/v3.1.0

  roles:
    - app-resources
    - webcert
