{
    "name": "webcert",
    "private": true,
    "scripts": {
        "test:e2e": "cypress run --reporter mocha-junit-reporter",
        "cy:verify": "cypress verify"
    },
    "devDependencies": {
        "cypress": "^3.1.4",
        "mocha": "^5.2.0",
        "mocha-junit-reporter": "^1.18.0"
    }
}
