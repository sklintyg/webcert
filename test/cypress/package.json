{
    "name": "cypress",
    "version": "0.1.0",
    "private": true,  
    "description": "Cypress tests",
    "scripts": {
	"test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
	"cypress": "3.1.4"
    }
}
