{
    "name": "webcert",
    "private": true,
    "scripts": {
        "test:e2e:chrome": "cypress run --browser chrome --reporter mocha-junit-reporter",
        "test:e2e:electron": "cypress run --reporter mocha-junit-reporter",
        "test:e2e:electron:record": "cypress run --reporter mocha-junit-reporter --record",
        "cy:verify": "cypress verify"
    },
    "devDependencies": {
        "cypress": "^3.2.0",
        "cypress-failed-log": "^2.5.0",
        "cypress-pipe": "^1.4.0",
        "cypress-plugin-retries": "^1.2.1",
        "mocha": "^6.1.4",
        "mocha-junit-reporter": "^1.22.0"
    }
}
