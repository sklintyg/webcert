description = 'Webcert (PU-integration)'

dependencies {
  compile "se.riv.population.residentmaster:population-residentmaster-schemas:${rivtaResidentMasterSchemasVersion}"

  compile "se.inera.intyg.common:common-support:${commonVersion}"

  runtime "org.springframework:spring-context-support:${springVersion}"
  runtime "net.sf.ehcache:ehcache:${ehCacheVersion}"

  compile "se.inera.intyg.common:common-cache-core:0-SNAPSHOT"

  testCompile "org.springframework:spring-test:${springVersion}"
  testCompile "org.hamcrest:hamcrest-all:${hamcrestVersion}"
}
