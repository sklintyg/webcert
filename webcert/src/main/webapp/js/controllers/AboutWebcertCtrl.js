define([
    'angular'
], function(angular) {
    'use strict';

    var moduleName = 'wc.AboutWebcertCtrl';

    angular.module(moduleName, []).
        controller(moduleName, [ function() {
        }]);

    return moduleName;
});
